import React from 'react'

// This is a top level page and should never be nested in another one.
// TOP LEVEL PAGE
const StepsPage = (): React.ReactElement => {
  return <div>Bientôt disponible&hellip;</div>
}

export default React.memo(StepsPage)
